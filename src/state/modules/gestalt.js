export const state = {
  gestalt: {
    description: "<span style='color:red;'>This is HTML in state</span>"
  }
};

export const mutations = {};

export const actions = {};

export const getters = {
  getGestalt(state) {
    return state.gestalt;
  }
};
